# usg-jupyter-configs

This repository retains JupyterHub-side deployment configuration.

The standalone `nblaunch` service code, tests, manifests, and service-local documentation now live in a separate repository. The remaining `nblaunch` content here is limited to Hub integration config under `jupyterhub/values-nblaunch.yaml` and repository-level validation around that config.
